export const metadata = {
  title: "Deal Pipeline — Track Revenue",
  description: "Track every deal from signal to closed revenue. Kanban pipeline with win-rate tracking and pipeline value metrics.",
};
export default function Layout({ children }) { return children; }
