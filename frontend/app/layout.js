import "./globals.css";
export const metadata = {
  title: "Signal CRM — Cross-Border Sales Intelligence",
  description: "Turn web signals into closed deals. Privacy-aware CRM for export teams, B2B agencies, and SaaS companies expanding internationally.",
};
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
