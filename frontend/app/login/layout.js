export const metadata = {
  title: "Sign In",
  description: "Sign in to Signal CRM — cross-border sales intelligence platform. Start your 14-day free trial.",
  robots: { index: true, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}
